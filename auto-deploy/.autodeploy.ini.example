# AutoDeploy Configuration Example
# 
# Place this file in the root of your service repository as '.autodeploy.ini'
# to customize the deployment behavior.

[settings]
# Specify a custom docker-compose file (optional)
# Single file:
compose_file=docker-compose.${ENVIRONMENT}.yml

# OR multiple files (comma-separated or multiple lines):
compose_files=docker-compose.yml,docker-compose.monitoring.yml
# compose_files=docker-compose.cache.yml

# Specify a custom Docker Compose project name (optional)
# If not set, will use: ${SERVICE}-${ENVIRONMENT}
# Supports environment variable substitution: ${VARIABLE}
project_name=my-project-${ENVIRONMENT}

# Specify a custom Docker image name (optional)
# If not set, will use the service name as image name
# Supports environment variable substitution: ${VARIABLE}
image_name=my-custom-image-${ENVIRONMENT}

# External images to load into buildx before build
# These images will be pulled and made available during build stages
# Useful for multi-stage builds that reference external images (COPY --from, FROM, etc.)
# Single image:
external_image=node:18-alpine

# OR multiple images (comma-separated):
external_images=node:18-alpine,nginx:alpine,redis:7-alpine

[external_images]
# OR list external images one per line in this section:
# node:18-alpine
# nginx:alpine
# redis:7-alpine
# postgres:15-alpine

[copy]
# Extra files/directories to copy from repo to deployment directory
# One path per line (relative to repository root)
scripts/
config/
docs/README.md

[render]
# Files where environment variables should be replaced
# ${VARIABLE_NAME} will be replaced with actual values from secrets
# One file per line (relative to deployment directory after copy)
config/app.conf
nginx/nginx.conf
docker-compose.yml
